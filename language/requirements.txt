# ============================================================
# requirements.txt — Verda VM / Dockerfile
# Source of truth: run_modal_multigpu.py image spec
# Python: 3.12   CUDA: 12.4
# ============================================================

# --- Core numerics (must come before torch) -----------------
numpy<2                  # torch 2.5 is incompatible with numpy 2.x

# --- PyTorch stack ------------------------------------------
# Install these SEPARATELY with the cu124 index URL:
#   pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 \
#       --index-url https://download.pytorch.org/whl/cu124
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1
triton==3.1.0

# --- ML / training ------------------------------------------
datasets==2.15.0
einops==0.7.0
fsspec==2023.10.0
h5py==3.10.0
hydra-core==1.3.2
lightning==2.2.1
omegaconf==2.3.0
packaging==23.2
pandas==2.2.1
rich==13.7.1
scikit-learn==1.4.0
transformers==4.38.2
torchmetrics==1.6.1
timm
wandb
huggingface-hub
hf_transfer
mauve-text            # required: main.py does `import mauve`

# --- Dev / monitoring (optional but useful on the VM) -------
ipdb==0.13.13
nvitop==1.3.2
seaborn==0.13.2
notebook==7.1.1

# --- Flash Attention ----------------------------------------
# DO NOT install via pip install flash_attn — it will try to compile from
# source and fail or take hours. Use the prebuilt wheel instead:
#
#   pip install --no-deps \
#     https://github.com/Dao-AILab/flash-attention/releases/download/v2.7.4.post1/flash_attn-2.7.4.post1+cu12torch2.5cxx11abiFALSE-cp312-cp312-linux_x86_64.whl
#
# This wheel is: CUDA 12.x | torch 2.5 | cxx11abi=False | Python 3.12
# --no-deps is intentional (matches Modal exactly — avoids torch re-install)
